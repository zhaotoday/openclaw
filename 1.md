# 任务：将现有 Content Script 浮窗交互改造为浏览器扩展 Popup

## 背景与术语（务必严格区分以下两个账号，不可混用）

- **乔威数据账号**：我们自己平台（jowaytech）的账号，登录态由 `@/stores/auth`（基于 `@joway/core` + background OAuth）维护。下文凡提到"登录 / 退出 / 用户名 / 管理面板"，均指此账号。
- **Temu 卖家账号**：第三方 Temu 卖家中心（`https://seller.kuajingmaihuo.com`）的账号，登录态通过 background 消息 `CHECK_TEMU_LOGIN_STATUS` 校验四个站点 cookie 是否全部有效。下文凡提到"Temu 登录 / 检测中 / 登录 Temu"，均指此账号。

> 严禁出现裸"账号"二字而不指明是哪一个；UI 文案与代码注释也都要用全称。

## 现状（可复用的实现）

- 入口：`src/entrypoints/temu.content/`（Content Script 浮窗）
  - `App.vue`：根组件，承载 `el-popover` + `FloatingTrigger`
  - `components/AuthPanel.vue`：乔威数据账号登录/已登录展示
  - `components/ActionPanel.vue`：包含"同步数据"按钮、Temu 登录态检测、Temu 登录引导弹窗
  - `components/SyncDataDialog.vue`：同步数据弹窗（核心业务面板，**整段复用**）
- 状态：`src/stores/auth.ts`（乔威数据账号）
- 消息：`sendMessage('CHECK_TEMU_LOGIN_STATUS', undefined)` 校验 Temu 登录态
- 配置：`wxt.config.ts` 的 `manifest.action`（需补 `default_popup`）

## 目标

新建 `src/entrypoints/popup/`（WXT Popup 入口），把上述交互全部迁移到浏览器扩展的 Popup 中实现，并按下面的 UI 规范重做布局。

## UI 规范

### 1. Popup 整体

- 尺寸：`width: 1600px; height: 1200px`（在 `index.html` 与根容器同时约束）
- 布局分为 **头部（Header）** 和 **内容区（Content）** 两块，垂直排列。

### 2. 头部（Header）

| 位置 | 状态 | 显示内容 |
| --- | --- | --- |
| 左侧 | 乔威数据账号未登录 | 文案：`未登录账号`（可带占位头像/图标） |
| 左侧 | 乔威数据账号已登录 | 显示乔威数据账号的用户名；鼠标悬浮时下拉菜单显示：`管理面板`（点击新窗口打开 `https://business.jowaytech.com/`）、`退出`（调用 `authStore.logout()`） |
| 右侧 | 任意 | 文案：`欢迎使用乔威数据 v{version}`，版本号取自 `browser.runtime.getManifest().version` |

### 3. 内容区（Content）

按"乔威数据账号登录态 → Temu 卖家账号登录态"两层判断渲染：

- **乔威数据账号未登录**：内容区居中显示
  - 占位插画
  - 文案：`请登录【乔威数据】`
  - 主按钮：`登录`（调用 `authStore.login()`）
- **乔威数据账号已登录**，进一步根据 Temu 卖家账号登录态：
  - **检测中**：居中显示 `请保持 Temu 卖家中心在线\n检测中...`（`\n` 表示换行）
  - **Temu 卖家账号已登录**：渲染 `SyncDataDialog.vue` 的内容区（不再以弹窗形式出现，而是作为 Popup 内嵌主面板）
  - **Temu 卖家账号未登录 / 检测失败**：居中显示
    - 文案：`请保持 Temu 卖家中心在线`（换行）
    - 主按钮：`登录 Temu`（点击新窗口打开 `https://seller.kuajingmaihuo.com/login`，可复用 `ActionPanel.vue` 中的引导步骤图与文案作为辅助说明）

## 行为规则

1. **Temu 卖家账号检测时机**：仅在"打开 Popup 且乔威数据账号已登录"时触发一次；不要再做轮询、也不要在乔威数据账号未登录时触发。
2. **乔威数据账号登录态变化**：保留 `authStore.startAuthDataWatch()` / `stopAuthDataWatch()` 的订阅机制，登录成功后自动触发一次 Temu 卖家账号检测。
3. **错误处理**：
   - 乔威数据登录失败：`ElMessage.warning('登录未完成，请重试')`
   - 乔威数据退出失败：`ElMessage.error('退出失败')`
   - Temu 检测异常：视为"未登录"分支处理，并打印 `console.error`

## 工程要求

1. **入口与配置**：
   - 在 `src/entrypoints/popup/` 下创建 WXT Popup 入口（`index.html` + `App.vue` + `main.ts`），按 WXT 约定即可被识别。
   - 在 `wxt.config.ts` 的 `manifest.action` 增加 `default_popup: 'popup.html'`（保留现有 `default_icon`）。
2. **复用与清理**：
   - 复用 `SyncDataDialog.vue` 的核心面板内容；如其原本依赖弹窗的部分（`v-model`、`<el-dialog>` 包裹）需要解耦，请抽出 `SyncDataPanel.vue` 而不是直接修改原文件，确保 Content Script 路径如仍被引用不受影响。
   - 检查并移除迁移到 Popup 后已无用的 Content Script 代码：`temu.content/App.vue`、`FloatingTrigger.vue`、`AuthPanel.vue`、`ActionPanel.vue`，以及 `entrypoints/content.ts` 中针对该浮窗的注册逻辑（若 `temu.content` 已不再需要，可整体删除）。
   - 不要清理仍在使用的 background、stores、utils、SyncDataDialog 业务逻辑等。
3. **样式**：使用 UnoCSS + Element Plus，保持与现有风格一致；Popup 根容器固定 1600 × 1200，并处理好滚动溢出。
4. **类型与注释**：
   - 全程 TypeScript，使用 `<script lang="ts" setup>` + Composition API。
   - 所有新建/修改的函数、组件、Store 与复杂逻辑使用 **TSDoc 中文注释**，风格参考现有 `App.vue` / `AuthPanel.vue` / `ActionPanel.vue`。
5. **保持原有功能可用**：同步数据弹窗内的全部业务逻辑、Temu 登录引导步骤图、跳转管理面板等行为均不可回退。

## 验收清单

- [ ] 点击扩展图标可打开 1600 × 1200 的 Popup
- [ ] 头部：未登录乔威数据时显示"未登录账号"；已登录时显示用户名 + 悬浮下拉（管理面板、退出）；右侧版本号正确
- [ ] 内容区按"乔威数据登录态 × Temu 登录态"四种组合显示正确文案 / 按钮 / 同步面板
- [ ] Temu 检测仅在打开 Popup 且乔威已登录时执行一次，不轮询
- [ ] 同步数据原有功能在 Popup 内嵌形态下完整可用
- [ ] 已删除迁移后不再使用的 Content Script 文件，且未误删共用代码
- [ ] 新增/修改文件均带中文 TSDoc 注释，无 ESLint / TS 报错
